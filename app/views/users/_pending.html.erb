<h3>Pending Requests</h3>
<ul>
  <%@pending_requests.each do |request| %>
    <li> 
      <%= request.user.name %>
      <%=link_to 'Accept Friend Request', friendships_path(id: request.user_id), method: :post %>
   </li>
   <%end%>
</ul>

